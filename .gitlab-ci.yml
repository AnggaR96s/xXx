py37_nose:
  image: python:3.7
  script:
    - apt-get update -q -y
    - apt install ffmpeg
    - pip install -r requirements.txt
    - pip install -r requirements-stdborg.txt
    - python -m stdborg test

