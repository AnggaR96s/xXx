CheckUserBotWorking:
  image: python:3.7
  script:
    - apt-get update -q -y
    - apt-get install wget ffmpeg -q -y
    - pip install -r requirements.txt
    - pip install -r requirements-stdborg.txt
    - python -m stdborg test

